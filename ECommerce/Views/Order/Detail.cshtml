@model ECommerce.Core.Model.Entity.Order
@{
                /**/

                ViewBag.Title = "Sipariş Detayı";
}

<h2>@Model.ID Nolu Siparişinizin Durumu:@Model.Status.Name</h2>
<hr />
<b>Banka Bilgileri</b>
<table>
    <tr>
        <td>Banka</td>
        <td>Banka İsmi</td>
    </tr>
    <tr>
        <td>Iban</td>
        <td>TR 16546486156365156</td>
    </tr>
    <tr>
        <td>Alıcı Adı</td>
        <td>Firma Bilgileri</td>
    </tr>
</table>
@{
    if (Model.StatusID == 1)
    {
        <a href="/Order/Pay?id=@Model.ID" class="btn btn-success">Ödeme Yaptım</a>
    }
}

<hr />
<b>Sipariş Bilgileri</b>
<table>
    <tr>
        <td>Toplam</td>
        <td>@Model.TotalPrice.ToString("##,## TL")</td>
    </tr>
    <tr>
        <td>Ürünler Toplamı</td>
        <td>@Model.TotalProductPrice.ToString("##,## TL") </td>
    </tr>
    <tr>
        <td>İndirim Toplamı</td>
        <td>@Model.TotalDiscountPrice.ToString("##,## TL")</td>
    </tr>
    <tr>
        <td>Vergi Toplamı</td>
        <td>@Model.TotalTaxPrice.ToString("##,## TL")</td>
    </tr>
</table>
<hr />
<b>Ürünler </b>
<table>
    <tr>
        <th>Ürün</th>
        <th>Adet</th>
    </tr>
    @foreach (var item in Model.OrderProducts)
    {
        <tr>
            <td>@item.Product.Name</td>
            <td>@item.Quantity</td>
        </tr>

    }

</table>

